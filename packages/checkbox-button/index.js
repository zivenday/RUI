import RCheckboxButton from '../checkbox/src/checkbox-button.vue';

/* istanbul ignore next */
RCheckboxButton.install = function(Vue) {
  Vue.component(RCheckboxButton.name, RCheckboxButton);
};

export default RCheckboxButton;
